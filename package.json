{
  "name": "@supabase-kit",
  "version": "1.0.0",
  "dependencies": {
    "zod": "^4.3.5"
  },
  "private": true,
  "scripts": {
    "dev": "bun run --filter './apps/*' dev",
    "build": "bun run --filter './packages/*' build",
    "test": "bun test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "typescript": "^5.9.3"
  }
}