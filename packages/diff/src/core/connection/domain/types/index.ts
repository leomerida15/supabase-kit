/**
 * Barrel export para tipos del dominio de conexión.
 *
 * @module core/connection/domain/types
 */

export type {
	ConnectionParams,
	DatabaseConnection,
	ConnectionConfig,
} from './connection.types.js';
