debes ejecutar un commando de CLI que ser 

```bash
bun run type:check
```
este script te retornara una validacion de tipado,
con esto seleccionas el archivo que muestra error de tipado e indica un plan de correccion que cumpla con el tipado de typescript
y pregunta si queremos soluicionar el error

luego preguntas si repetimos el proceso.